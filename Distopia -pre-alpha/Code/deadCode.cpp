    /*
    vector<Collider> vec = w.collision(tmp);

    for(unsigned int i = 0; i < vec.size(); i++)
    {
        switch(vec[i].vertical)
        {
        case N_Side::Both:
        case N_Side::Bottom:
        case N_Side::Top:
            tmp.c_y = position.c_y;
            break;
        }
        switch(vec[i].horizontal)
        {
        case N_Side::Both:
        case N_Side::Left:
        case N_Side::Right:
            tmp.c_x = position.c_x;
            break;
        }
    }*/